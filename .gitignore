# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies & build output
node_modules
.grok-app-dev-home
.cargo-home
dist
dist-ssr
dist-installers
*.local
# Root uses pnpm (pnpm-lock.yaml). Do not reintroduce npm's lock at repo root;
# remote-bridge/ may keep its own package-lock.json for independent npm installs.
/package-lock.json

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# Tauri / Rust
src-tauri/target
src-tauri/gen/schemas
# Generated by scripts/build-release-config.mjs (contains pubkey; not secrets)
src-tauri/tauri.release.conf.json
# Generated by release.yml when WINDOWS_CERTIFICATE is set (thumbprint only)
src-tauri/tauri.windows.sign.conf.json

# Local runtime data & secrets
*.db
*.db-*
.env
.env.*
*.pem
*.p12
*.pfx
*.key
/certificate/
auth.json
secrets.json
config.local.*

# Scratch / local experiments (not part of the published tree)
.spike-proj
.refs
.codex/
.tmp*/
tmp/
temp/
.cache/
.turbo/
.eslintcache
*.tsbuildinfo

# Local agent / IDE workspaces
.claude/
.zcode/

# Playwright MCP local dumps
.playwright-mcp/

# ChatCut upstream clone (re-fetched by scripts; pin in vendor/chatcut-agent-plugin.pin)
vendor/chatcut-agent-plugin/
vendor/chatcut-grok-adapted/

# v0.2.26 发布前审查诊断（v0.2.25..HEAD）

> 生成时间：2026-08-24 08:21 (UTC+8)
> 审查执行：Cursor Agent（主导）+ pi 0.84.2（当时按已废止的 AGENTS.md §8 强制审核）
> pi 原始审查记录：`/tmp/pi-review-v0226-range.txt`（stdout 存档）

## 1. 审查范围

- 基线 tag：`v0.2.25`，HEAD：`1eb86ffd`（main）
- **17 个提交，210 个文件，+5661 / −1160**
- 提交清单：

| 提交 | 说明 |
|------|------|
| `1eb86ffd` | fix(terminal): stable tab numbers, real close, 5px inset |
| `ada590d8` | fix(side-workbench): keep new-session picker and center Plan empty |
| `fb843d9b` | fix(session,files): compact timeline anchors + live files-tree refresh |
| `ffd82062` | perf(chat): keep long-transcript scroll off the urgent lane (#854) |
| `b7316998` | feat: import Grok Build CLI sessions from Account recent logs (#862) |
| `18096965` | fix(remote-im): authenticate WeCom webhook and make the sender ACL fail-closed (#851) |
| `d3f56d78` | 恢复侧栏平滑开合并稳定标题栏布局 (#858) |
| `4d8532cc` | 优化新对话欢迎动画并增加外观开关 (#857) |
| `064a4376` | 修复设置页签闪烁并统一胶囊切换动画 (#856) |
| `71428d23` | 为深浅主题切换增加平滑过渡并保持侧栏模糊 (#860) |
| `a6051b70` | 增强壁纸下设置页面的背景模糊 (#859) |
| `a99250fb` | 消除宠物身体外缘白色光晕 (#861) |
| `e0144b0e` | fix(sidebar): tidy L1 project header icons and overflow menu |
| `71ac317e` | fix(sidebar): retarget L1 projects reveal when content shrinks |
| `5c22b8ea` | fix(composer): match input width to chat reading column after first turn |
| `557d51e5` | fix(official-aux): inject X/Imagine on packaged custom mains without ChatCut |
| `9f097249` | perf(chat): keep long-transcript scroll off the urgent lane (#853) |

## 2. 基线自测（发布范围 HEAD）

| 检查 | 结果 |
|------|------|
| `pnpm typecheck` | ✅ 0 错误 |
| `pnpm test` | ✅ 505 文件 / 6575 用例全过（222s） |
| `cargo test remote_im`（pi 执行） | ✅ 138 过 |
| `cargo test` 全量 | ⏳ 后台任务因 Cursor 沙箱助手启动失败（ENOENT）未跑成，验收阶段重跑 |

## 3. pi 审查结论

### BLOCKER（必须修）

**无。** 重点疑点均已在代码中闭环：

- **WeCom 鉴权（#851 / `18096965`）**：`callback_token` 缺失直接拒启（`wecom.rs:109-115`）；验签为官方 SHA1(排序 [token,timestamp,nonce,payload]) + 常时比较（`wecom_signature_ok` + `const_time_eq`，`wecom.rs:160-179`）；GET verify 只在验证通过后才回显 `echostr`（旧代码无条件回显，已修）；路径解码后精确匹配，404/401 分明；6 个单测覆盖坏签/缺签/空签。
- **ACL fail-closed**：`allow_from_list` 缺省从 `"*"` 改为 `""`（`outbound.rs:271-289`）；runtime 启动前再拦一道 `allow_from_blocks_enable`（`runtime.rs:66-77`）。
- **CLI 会话导入（#862 / `b7316998`）**：`import_cli_session` 先 `validate_agent_session_id` 拒绝 `..`/斜杠穿越（`cli_sessions.rs:1800`）；自动建项目排除根目录/home/祖先目录且**永不自动信任**（`cli_sessions.rs:190-223`）。
- **规范项**：`src/App.tsx` 范围内 0 行改动（增长冻结 ✅）；无新增原生 `<select>`、无 `window.confirm/prompt/alert`（仅注释提及）；新菜单 `SidebarProjectsMoreMenu` 走 portal + 实心面板（`sidebar.part3.css:67`），非透明菜单。

### WARNING（6 条）

| # | 问题 | 位置 | 处置 |
|---|------|------|------|
| 1 | WeCom 验签无 timestamp 新鲜度校验，重放窗口无限 | `src-tauri/src/remote_im/channels/wecom.rs` `wecom_signature_ok()` | **已委派 Grok Build 修复**（±300s 窗口 + 单测） |
| 2 | webhook 默认绑 loopback，公网回调静默收不到，无提示 | 同上 `run_webhook()` + 设置页 Remote IM | **已委派 Grok Build 修复**（运行状态提示 + i18n 文案 15 locale） |
| 3 | `should_auto_add_project_path` 段数阈值（>=4 段）在 Linux/Windows 误收浅层目录 | `src-tauri/src/cli_sessions.rs:190-215` | **已委派 Grok Build 修复**（改按相对 home 深度 >=2） |
| 4 | WebKit 主题过渡 `captureVisibleThemeFrames` 对 `body *` 全量快照，大 DOM 卡帧 | `src/lib/theme.ts`（`71428d23` 新增） | **已委派 Grok Build 修复**（元素数上限，超出降级为无动画） |
| 5 | #851、#862 合并时按维护者要求跳过 pi 单项审查（当时违反 AGENTS.md §8） | commit message 记录 | 当时范围级 pi 审查视为补审；强制 pi 规则现已废止 |
| 6 | `CHANGELOG.md` 顶部仍是 `[Unreleased]` | `CHANGELOG.md` | 打 tag 前改为 `## [0.2.26] - 日期`（release-tag.sh / CI 均会强制校验） |

### OK 结论（pi 逐项取证）

| 检查项 | 结论 | 证据 |
|--------|------|------|
| 明显 bug / 回归 | ✅ 无阻断级问题 | typecheck 0 错；6575 测试全过；`planFocusKey=0` 修复（`planModePro.ts:233-235`）；终端真杀 PTY（`pty_host.rs:370-389`，clone_killer + SIGKILL 进程组，pid>1 保护）；stick-to-bottom 重构保留 scroll-up 逃逸语义 |
| i18n 锁步 | ✅ | account/sidebar/composer/workspace/settings 五文件 15 locale key 集合 diff=0；抽查 zh/ja/de/ta 为真实翻译 |
| 禁用控件/对话框 | ✅ | 无原生 select；window.confirm 仅存于注释；新菜单实心面板 + portal |
| settingsCatalog 注册 | ✅ | `appearance.welcomeMotion` 已注册（`entries/appearance.ts:163-182`），15 locale 关键词齐 |
| App.tsx 冻结 | ✅ | 本范围零改动，新状态落在 hooks/components/lib |
| 安全 | ✅（附 WARNING 1/2） | 验签/常时比较/fail-closed ACL/loopback 默认均有测试；diff 扫描无密钥、无 auth.json/.env |
| CHANGELOG 一致性 | ✅ | Unreleased 20 条条目逐一对应 17 个提交，无漏记多记 |

**pi 总结论：可发布。** WARNING #6 为打 tag 前必做；#1/#2 本次修（已委派），#3/#4 一并修。

## 4. 修复委派状态（Grok Build）

WARNING 1–4 已委派 `grok -p --always-approve` 执行（用户已批准）。当前工作区状态（**已改完、未提交**）：

- `src-tauri/src/remote_im/channels/wecom.rs` + `config.rs` — 重放窗口 + loopback 提示后端
- `src-tauri/src/cli_sessions.rs` — 浅层目录判定
- `src/lib/remoteIm/channelHealth.ts` / `store.ts` / `wecomConfig.ts` + 各自测试 — loopback 提示前端
- `src/i18n/messages/*/settings-remoteIm.ts` — 15 locale 提示文案
- `src/lib/theme.ts` + `theme.test.ts` — 快照元素数上限
- `CHANGELOG.md` — Unreleased 补条目

## 5. 验收清单（下一步）

- [ ] 逐文件 review Grok Build 的 diff（尤其 wecom.rs 验签窗口边界、i18n 15 locale 锁步）
- [ ] `pnpm typecheck && pnpm test` 全绿
- [ ] `src-tauri` `cargo test` 全绿（重跑，上次沙箱故障未执行）
- [ ] 提交（如 Grok Build 未提交则由验收方提交）

## 6. 发布步骤（验收通过后）

1. `CHANGELOG.md`：`[Unreleased]` → `## [0.2.26] - 2026-08-24`（重建空 Unreleased 骨架）
2. `export GITHUB_TOKEN="$(gh auth token)"; python3 scripts/update-contributors.py`（README 有 diff 则单独 commit）
3. `./scripts/release-tag.sh 0.2.26` 确认后 `--push`
4. Grok Build 盯 CI：Actions `release` 四 job（macOS-ARM64 / macOS-x64 / Windows-x64 / Linux-x64）全绿；Release 资产齐（2 dmg + setup.exe + portable.zip + AppImage + deb + rpm + 稳定别名 + downloads.json）；Release body 仅为 0.2.26 章节
5. 发版后：README 贡献者画廊已刷新、版本号与 tag 一致、branch hygiene

## 7. 已知环境问题（记录）

- GitHub SSH（端口 22）间歇性断连，`git fetch`/push 可能需重试；本地 main 领先 origin/main 2 个提交（审查开始时）。
- Cursor 沙箱助手一次 ENOENT 导致后台 cargo test 未执行；重跑即可。
- Cursor 审批卡片组件多次内部报错（"Could not find bubble for toolCallId"），委派 Grok Build 时改用会话内确认。

## 结论：**通过（无 blocker）**

审查对象：暂存的 WP-W26 + WP-W27（#870 余项）。`pi -p`，工具仅 `read`/`bash`。pi 警告 `No models match pattern "xai-oauth/grok-4.5"` 后仍完成审查，exit 0。

`python3 scripts/check-code-quality-gates.py` 全部 PASS；`tsc --noEmit`、eslint（改动文件）、vitest（4 个相关测试文件 21 tests）均通过。

### 核对表

| 约束 | 结果 | 证据 |
|---|---|---|
| 不新增 feature `useState` 到 App.tsx / AppWorkbench.tsx | ✅ | AppWorkbench 净删 ~50 个 useState，改为解构 `useAppSettingsPrefs()`；App shell 仅 18 行 |
| 行数 / useState / useEffect 只降不升（基线 15612 / 209 / 81） | ✅ | 实测 **15183 / 155 / 80**；ceilings 15350/160/83 |
| 新文件 <1000 行；files_ge_1000 ≤69 | ✅ | useAppSettingsPrefs.ts 259、useSessionConnect.ts 419、appSettingsHydrate.ts 191；count=69 |
| i18n createT/t()；无 window.confirm/prompt/alert | ✅ | gate `NO_WINDOW_DIALOGS clean`；hook 内用户文案经 `host.tr` |
| 无原生 select / OS 菜单 | ✅ | 新增文件均为纯逻辑/hook |
| 改 nav/prefs/connect/liveMap 不必打开 AppWorkbench 函数体 | ✅ | parse / hydrate / connect+liveMap 均在域模块；host 只接线 |
| host 仍在 refreshLists 调 hydrateFromSettings | ✅ | AppWorkbench mirror + bootstrap 两处 |
| sessionConnectHostRef 每次渲染填充 | ✅ | 与 sessionNavHost 同一模式 |
| ensureConnectCountRef / syncEnsureConnectingUi 由 hook 导出且 host 使用 | ✅ | hook 返回；host 解构并用于 in-flight / UI 投影 |

### 行为风险（非 blocker）

- connect 竞态防护逐字搬迁（per-session claim、120s 等待、draft 物化三重迁移、`shouldAdoptView`）。
- prefs hydrate：mirror 分支现在一次全量 hydrate，比旧实现更早收敛到终值。
- `useSessionConnect.test.ts` 为 smoke（claim/release）；ensureConnected 快路径/并发等待建议后续补，不挡本次落地。

无 blocker，该项可标完成。
